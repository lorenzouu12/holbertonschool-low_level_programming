#include <stdio>

/**
 * main - Entry point
 *
 * Descrption: Print the alphabet in lower case
 *
 * Return: Always 0
 */

int main(void)
{
char letter = 'a';

while (letter <= 'z')
{
puchar (letter);
letter++;
}
putchar('\n');

return (0);
}
